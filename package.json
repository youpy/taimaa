{
  "name": "ya-stable-timer",
  "version": "0.0.2",
  "description": "Yet another stable timer implementation using `ended` event fired by AudioBufferSourceNode",
  "scripts": {
    "test": "NODE_ENV=test mocha --growl",
    "test:watch": "yarn test -- --watch",
    "build": "babel src --out-dir lib",
    "prepublish": "npm run test && npm run build"
  },
  "files": [
    "package.json",
    "README.md",
    "index.js",
    "lib"
  ],
  "main": "index.js",
  "author": "youpy <youpy@buycheapviagraonlinenow.com>",
  "license": "MIT",
  "dependencies": {},
  "devDependencies": {
    "babel-cli": "^6.18.0",
    "babel-preset-es2015": "^6.18.0",
    "babel-preset-power-assert": "^1.0.0",
    "babel-preset-stage-0": "^6.16.0",
    "babel-register": "^6.18.0",
    "mocha": "^3.2.0",
    "power-assert": "^1.4.2",
    "web-audio-test-api": "^0.5.2"
  }
}
